import { NormalizedStoriesEntry } from '@storybook/core-common';
interface Meta {
    title?: string;
}
export declare const autoTitleFromEntry: (fileName: string, entry: NormalizedStoriesEntry) => string;
export declare const autoTitle: (meta: Meta, fileName: string) => string;
export {};
