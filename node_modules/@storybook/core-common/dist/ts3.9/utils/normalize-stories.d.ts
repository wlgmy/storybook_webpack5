import type { StoriesEntry, NormalizedStoriesEntry } from '../types';
export declare const normalizeStoriesEntry: (entry: StoriesEntry, configDir: string) => NormalizedStoriesEntry;
interface NormalizeOptions {
    configDir: string;
    workingDir: string;
}
/**
 * Stories entries are specified relative to the configDir. Webpack filenames are produced relative to the
 * current working directory. This function rewrites the specifier.directory relative to the current working
 * directory.
 */
export declare const normalizeDirectory: (entry: NormalizedStoriesEntry, { configDir, workingDir }: NormalizeOptions) => NormalizedStoriesEntry;
export declare const normalizeStories: (entries: StoriesEntry[], options: NormalizeOptions) => NormalizedStoriesEntry[];
export {};
